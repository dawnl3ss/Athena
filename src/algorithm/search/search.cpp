#include "search.h"

bool search::linear_search(int haystack, int needle) {

    for (unsigned int i = 0; i < vecSize; i++){
        std::cout << array.at(i) << " ";
    }
    std::cout << std::endl;
    return true;
}